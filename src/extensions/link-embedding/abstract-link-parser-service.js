'use strict';

/**
 * Abstract Link parser service.
 */
var AbstractLinkParserService = function() {
};
module.exports = AbstractLinkParserService;


/**
 * Called when a link information needed to be parsed.
 * @param {string} unusedUrl
 */
AbstractLinkParserService.prototype.parse = function(unusedUrl) {
};
